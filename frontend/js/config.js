// This file contains the configuration for the frontend.
// In a real production environment, you might want to use environment variables or a build-time replacement strategy for these values.

const AppConfig = {
    // The base URL of the backend API.
    // If your frontend and backend are served from the same domain, you can use a relative path like '' (empty string).
    // For development, when the backend runs on a different port (e.g., 8080), specify the full URL.
    apiBaseUrl: 'http://localhost:8001' 
};
